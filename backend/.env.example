# Porta onde a API vai rodar
PORT=5000

# Chave da API da Anthropic, usada pelo Assistente IA.
# Sem essa chave, o resto do app funciona normalmente -
# só o chat de IA fica desativado (com uma mensagem amigável).
# Pegue a sua em https://console.anthropic.com/
ANTHROPIC_API_KEY=
